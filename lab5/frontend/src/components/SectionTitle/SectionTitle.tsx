import SectionTitleLayout from "./SectionTitleLayout";

const SectionTitle = () => {
    return (
        <SectionTitleLayout>
            <h1>Paste the URL to be shortened</h1>
        </SectionTitleLayout>
    )
}

export default SectionTitle;